import { Typography } from "@mui/material";

/**
 * UNUSED PAGE — not referenced by routes or the sidebar navigation.
 * [DATA: MOCK/EMPTY] static placeholder heading only; no data wiring.
 */
const PowerStationsPage = () => {
  return <Typography variant="h4">Power Stations</Typography>;
};

export default PowerStationsPage;