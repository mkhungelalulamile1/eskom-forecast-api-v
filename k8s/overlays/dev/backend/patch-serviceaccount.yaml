# id-coalstockpile-dev (SAN-DTI-NONPROD-RG) -- created and federated to
# system:serviceaccount:app-coal-stockpile:eskom-forecast-sa (confirmed live).
# Its Key Vault Secrets User / Storage Blob Data Contributor role
# assignments are still outstanding -- the account that created this
# identity lacks Microsoft.Authorization/roleAssignments/write on the vault
# and storage account, same as aks-mssql-setup hit for this identical vault.
# See docs/RUNBOOK.md for the exact commands someone with Owner/User Access
# Administrator on those two resources still needs to run.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eskom-forecast-sa
  annotations:
    azure.workload.identity/client-id: "75399a2c-0c4b-4b55-a568-89763fb6d6eb"
